import { createSectionScene } from './createSectionScene';

export default createSectionScene('groundTruth', 'AnIML Ontology as a Ground Truth', {
	accentColor: '#3A94C5',
	transitionFromAccentColor: '#DFA000',
});
